%h1= render @conference

.clear
.buttons.grid_3.prefix_2= link_to t('sessions.new?'), new_conference_session_path(@conference)
.buttons.grid_3= link_to t('registrations.new?'), new_conference_registration_path(@conference)
.clear

- @sessions.each do |session|
  = render 'sessions/header', :session => session
  = render 'sessions/summary', :session => session
  
.pagination
  = will_paginate @sessions
  
