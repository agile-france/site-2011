= render @session

%p
  = link_to t('common.update'), edit_conference_session_path(@session.conference, @session) if can_edit?(current_user, @session)
  = link_to t('common.back'), conference_sessions_path(@session.conference)