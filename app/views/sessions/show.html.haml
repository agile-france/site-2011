=render @session

%p.pad_2
  =link_to t('common.update'), edit_awesome_session_path(@session) if can? :edit, @session
  =link_to t('common.back'), @session.conference
  =link_to t('common.delete'), awesome_session_path(@session), :method => :delete, :confirm => t('sessions.seppuku?') if can? :destroy, @session