%h3= @session.title
%p= @session.description

%p
  = link_to t('common.update'), edit_conference_session_path(@session.conference, @session)
  = link_to t('common.back'), conference_sessions_path(@session.conference)